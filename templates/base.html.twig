<!DOCTYPE html>
<html lang="fr">
    <head >
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>&#128007</text></svg>">
        <!-- CSS only -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">

        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    {% block body %}
    <body class="container-fluid">
        {% if app.user %}
            <nav class="navbar navbar-expand-lg navbar-light bg-light " >
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('app_accueil') }}"><img src="{{ asset('img/logoGTOsortir-smal.png') }}" alt="logo"></a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                        <div class="navbar-nav">
                            <a class="nav-link" href="{{ path('app_villes') }}">Villes</a>
                            <a class="nav-link" href="{{ path('app_sites') }}">Sites</a>
{#                            <a class="nav-link" href="{{ path('app_sortie_edit') }}">Sortie</a>#}
                            <a class="nav-link" href="{{ path('app_sortie_index') }}">Accueil</a>
                            <a class="nav-link" href="{{ path('app_profil_view') }}"><img class="icon" src="{{ asset('img/profile_contact_account_man_icon_229517.svg') }}" alt="profil">Mon profil</a>

                            <a class="nav-link" href="{{ path('app_logout') }}"><img class="icon" src="{{ asset('img/4115235-exit-logout-sign-out_114030.svg') }}" alt="deconnexion">Deconnexion</a>
                        </div>
                    </div>
                </div>
            </nav>
        {% endif %}
        {% block main %}
            <main >
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="{{ path('app_logout') }}">Deconnexion</a>
                    {#        {% if is_granted("ROLE_ADMIN") %}#}
                    {#            <a href="{{ path('app_admin') }}">Administration</a>#}
                    {#        {% endif %}#}
                    {#    {% else %}#}
                    {#        <a href="{{ path('app_register') }}">Création de compte</a>#}
                {% endif %}
            </main>
        {% endblock %}
        <footer>&#128007 Projet ENI 2022 - Les lapins sont suppérieurs aux loutres ! - Propulsé par Titi la loutre, BichAchu, et Loulou le loup</footer>
    </body>
    {% endblock %}


</html>
